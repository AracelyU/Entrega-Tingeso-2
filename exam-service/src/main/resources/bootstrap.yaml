spring:
  application:
    name: exam-service
  cloud:
    config:
      enabled: true
      uri: http://localhost:8081
  datasource:
    url: jdbc:mysql://localhost:3306/examen
    username: root
    password: 2190
  jpa:
    hibernate:
      ddl-auto: create